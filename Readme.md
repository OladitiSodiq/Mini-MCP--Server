# Simple MCP Server (Model Context Protocol)

A lightweight **Model Context Protocol (MCP) server** built with **TypeScript + FastMCP**, providing common utility tools such as weather lookup, calculator, currency conversion, time zone handling, and unit conversion.

This server is compatible with **Claude Desktop** and the **official MCP Inspector**.